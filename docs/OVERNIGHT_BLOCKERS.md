# Overnight Backend Blockers

## 2026-08-25 — Live TrueForge session bootstrap validation failure

| Field | Recorded outcome |
| --- | --- |
| Capability | Create a real inline TrueForge session for the bounded read-only Investigator |
| Configuration method | Server-only `TRUEFORGE_BASE_URL`, `TRUEFORGE_MODEL`, and `TRUEFORGE_GITHUB_MCP_NAME`; `TRUEFORGE_TOKEN` omitted in standalone no-auth mode |
| Preflight status | `GET /healthz` returned HTTP 200 without an `Authorization` header; `GET /api/v1/mcp-servers/github/tools` also returned HTTP 200 and returned a GitHub tool catalogue |
| Attempt | SentinelForge submitted one source-verified `POST /api/v1/sessions` request with an inline agent spec, `github` MCP restricted to `@read-only`, sandbox disabled, dynamic subagents disabled, and structured JSON output requested |
| Exact result | The initial `agent.spec` request returned `TrueForge request failed for /api/v1/sessions: HTTP 422.` A single source-verified envelope test against the official `v0.1.4` tag used `agent_spec` and returned HTTP 400 with `{"error":{"message":"✖ Unrecognized key: \"agent_spec\"\n✖ Invalid input\n  → at agent"}}`. |
| Session created | No. The runtime returned no session ID, no turn was started, no MCP tool was invoked by an agent, no sandbox was used, and no GitHub write occurred. |
| Impact | Real session, turn streaming, real MCP evidence ingestion, sandbox verification, approval continuation, and GitHub PR execution remain **blocked**. The deterministic offline fixture remains available and unchanged. |
| Next move | The tunnel-exposed runtime explicitly expects the newer `agent.spec` envelope generated by the installed `@truefoundry/trueforge-sdk@0.1.3`, while the public `v0.1.4` source tag expects `agent_spec`. The adapter is restored to the live runtime’s confirmed `agent.spec` envelope. One final controlled attempt may capture the remaining HTTP 422 detail through the now-sanitizing client; no turns, sandbox, or GitHub write will be attempted unless session creation succeeds. |

The first failed code path exposed the local transition guard (`CREATED → FAILED`) rather than the remote validation error. That guard was corrected to permit terminal bootstrap failures, and the subsequent controlled attempt recorded the remote HTTP 422 status shown above. The server-envelope discrepancy is a verified **version/contract mismatch risk**: the public repository tag does not match the request envelope actively accepted by the tunnel-exposed runtime.

## 2026-08-25 — Live TrueForge sandbox capability remains unverified

| Field | Recorded outcome |
| --- | --- |
| Capability | Run one bounded TrueForge sandbox verification command |
| Command | `printf sentinel-forge-sandbox-ok` |
| Isolation | A dedicated live TrueForge session with sandbox enabled, no GitHub MCP attachment, no repository access, no credentials, no network task, and no write action |
| Session/turn result | A real sandbox-probe session and turn were created and correlated successfully after removing the erroneous one-session-per-mission database constraint. |
| Observed outcome | The turn reached `turn.done`, but emitted no sandbox lifecycle event. SentinelForge persisted the probe as `UNKNOWN`, not `PASS`. |
| Provider conclusion | **UNAVAILABLE / UNVERIFIED.** The runtime did not provide evidence that a sandbox provider executed the harmless command. The official v0.1.4 source configures server sandboxes only when sandbox settings and provider credentials are present; this runtime’s health and event APIs do not expose that configuration. |
| Impact | Real sandbox verification, a repair proposal, approval continuation, and GitHub PR execution remain blocked. SentinelForge will not represent the deterministic fixture verifier as a real TrueForge sandbox run. |
| Next move | Configure and validate the live TrueForge sandbox provider (for example, its documented Daytona settings) on the runtime side, then repeat this exact harmless probe before any repair or approval work. |

### Retry outcome — provider configured but sandbox bootstrap cannot install dependencies

At the user’s request, SentinelForge repeated the exact same isolated `printf sentinel-forge-sandbox-ok` probe. This time, the live runtime created a sandbox-enabled session and the model issued the actual `truefoundry-system` `exec` tool call. The provider then returned a concrete failure while initializing the sandbox environment:

> `Sandbox initialization failed: Failed to pip install pydantic>=2.0.0,<3.0.0 into sandbox .venv` because the sandbox’s configured proxy closed the connection to the Python package index.

The remote turn had not emitted `turn.done` when the bounded client request timed out, but its read-only event history contains two real `exec` attempts and the same provider bootstrap error. This is stronger evidence than the first probe: **the sandbox path is enabled, but it is not usable until its proxy/network or prebuilt environment can satisfy the required `pydantic` dependency.** No GitHub MCP connector, repository access, credentials, or write action was attached to this retry.

| Updated next move | Configure the sandbox image with compatible `pydantic` already installed, or restore the sandbox provider’s package-index proxy/network access. Then rerun the same harmless command and require an observed successful tool response plus terminal `turn.done` before unblocking verification. |

## 2026-08-25 — Real GitHub MCP investigation found no repairable repository content

The bounded, read-only Investigator successfully created a live TrueForge session and turn, initialized the GitHub MCP connector, and made real read-only calls including `get_file_contents`, branch, tag, release, and commit reads. The connector returned that `Aayushashsahu/sentinelforge` is empty, with no default branch, tags, releases, commits, or readable `README.md`. SentinelForge persisted observed MCP evidence separately from the model’s root-cause inference and advanced the mission only to `PLANNING_FIX`.

Because there is no source, workflow, or reproducible CI failure in the target repository, there is no bounded repair proposal to verify, approve, or write. No repair-engineer session, approval request, GitHub branch, commit, pull request, merge, deployment, workflow modification, repository administration action, or secrets action was performed.

## 2026-08-25 — Incident-fixture Investigator evidence limitation

SentinelForge created the public `Aayushashsahu/sentinelforge-incident-fixture` repository and verified that its deterministic initial `npm test` exits non-zero for the intended `package.json` `1.4.0` versus `release-manifest.json` `1.3.0` mismatch.

Two real read-only TrueForge Investigator sessions were attempted. The first initialized GitHub MCP and called `get_file_contents` for each required file plus branch and release reads. The connector returned success messages and file SHAs but did not return file text to the model; the model correctly reported insufficient evidence rather than inventing contents. The second session transcribed the repository name as `sentinelforce-incident-fixture` (with `force` rather than `forge`) despite the mission and prompt containing the correct name, and its MCP calls returned GitHub 404 responses. Both turns reached a real `turn.done`; no sandbox, approval continuation, branch, commit, pull request, or other GitHub write was issued.

**Impact:** GitHub MCP read connectivity is real, but the current model/tool interaction cannot yet produce a fully evidence-backed file-content root cause. The fixture remains publicly accessible and non-empty. The next safe work is a bounded Repair Engineer contract and verifier abstraction driven by the explicit fixture incident, while keeping its status non-live until direct MCP file evidence is available.

### Bounded Repair Engineer result

One separate real Repair Engineer session was created with only the GitHub MCP `@read-only` policy and sandbox disabled. It made real `get_file_contents` and branch reads but again transcribed `sentinelforge-incident-fixture` as `sentinelforce-incident-fixture` on follow-up calls. Its final output explicitly declared that file bodies were unavailable and returned `patch: null`, an empty changed-file list, a non-enum risk, and a string rather than array evidence limit. SentinelForge’s strict proposal parser rejected that output and recorded the mission as failed-safe. No patch was persisted, applied, approved, or sent to GitHub.
