import { Link } from "wouter";
import { ArrowLeft, CheckCircle2, CircleDashed, ShieldCheck } from "lucide-react";
import { trueForgeReadiness } from "../../../shared/sentinelforge";

export default function Readiness() { return <div className="forge-page max-w-5xl space-y-6"><Link href="/" className="-ml-3 inline-flex h-9 items-center rounded-md px-3 text-sm font-medium text-slate-300 transition-colors hover:bg-white/5 hover:text-white"><ArrowLeft className="mr-2 h-4 w-4" /> Mission dashboard</Link><section className="rounded-[28px] border border-white/10 bg-slate-950/70 p-7 md:p-10"><p className="eyebrow">Honest integration boundary</p><h1 className="mt-3 text-4xl font-semibold tracking-[-0.04em] text-white">TrueForge and MCP readiness</h1><p className="mt-4 max-w-3xl text-base leading-7 text-slate-300">SentinelForge is structured for a TrueForge-centered workflow, but this deployed demonstrator does not claim access to user tools, live GitHub MCP operations, or a provider sandbox. Every unavailable capability remains visibly labelled.</p><div className="mt-8 overflow-hidden rounded-2xl border border-white/10">{trueForgeReadiness.map(item => <div key={item.capability} className="grid gap-4 border-b border-white/[0.07] p-5 last:border-0 md:grid-cols-[1fr_auto_1.5fr]"><div className="font-medium text-white">{item.capability}</div><div className={`flex items-center gap-2 text-xs font-semibold ${item.status === "READY_TO_CONNECT" ? "text-emerald-200" : "text-amber-200"}`}>{item.status === "READY_TO_CONNECT" ? <CheckCircle2 className="h-4 w-4" /> : <CircleDashed className="h-4 w-4" />}{item.status.replaceAll("_", " ")}</div><div className="text-sm leading-6 text-slate-400">{item.detail}</div></div>)}</div></section><section className="rounded-2xl border border-amber-300/20 bg-amber-300/[0.07] p-6"><div className="flex gap-3"><ShieldCheck className="mt-1 h-5 w-5 shrink-0 text-amber-300" /><div><h2 className="font-semibold text-amber-100">Live integration admission criteria</h2><p className="mt-2 text-sm leading-6 text-slate-300">Before a live provider status can be shown, connect credentials server-side, scope GitHub tools to a selected repository, use a provider sandbox for generated code, add an end-to-end integration test, and retain the approval gate as the only route to a write action.</p></div></div></section></div>; }
